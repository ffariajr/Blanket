# Bugs Found — Pre-Release Testing

Every finding from executing `TESTING_PLAN.md` goes here. One entry per bug,
using the template below. Do not fix inline while testing (except
`TESTING_PLAN.md`'s Section G, which is fixed directly, not logged here) —
record for Fernando to triage. Order: append new entries at the bottom in
discovery order; don't renumber existing ones.

**If you're reading this file without having read `TESTING_PLAN.md` first,
go read that first** — it has the full ground rules every tester must follow
(never touch spreadsheet id=13, collision-resistant test-data naming,
exact-ID-only cleanup, which real-browser technique to use and how, exact
deploy/git commands) and the full scope this file's findings come from. This
file is just the output; the plan is the instructions.

Every entry must have gone through the same adversarial-verify step used
throughout this project: a DIFFERENT agent than the one who found it attempts
to independently reproduce it from scratch before `Verified` is set to `yes`.
An unconfirmed finding still gets logged (mark `Verified: no — see detail`),
don't discard it silently.

---

## Template (copy this block per finding)

### [NNN] Short one-line summary

- **Area:** (e.g. Formula engine / Mobile touch / Security / Performance / WS collab)
- **Severity:** critical / high / medium / low
- **Found by:** (dimension/agent name or description)
- **Verified:** yes (by: ...) / no — see detail
- **Reproduction:**
  1. ...
  2. ...
- **Expected vs. actual:**
- **Verify detail:** (what the independent verifier actually did and observed)
- **Status:** open / fixed (commit `...`) / triaged-wontfix (reason) / needs-Fernando-decision

---

## Findings

_(none yet — testing execution appends below this line)_

### [001] Editing client's own cell can show a stale value after a same-cell race, with no self-correction

- **Area:** WS collab (Section E — race conditions, rapid-fire same-cell edits from 3+ clients)
- **Severity:** medium
- **Found by:** Section E WS matrix subagent, "race conditions" item — 3+ real Chromium clients (puppeteer-core), same tab_id, rapid-fire edits to the same cell
- **Verified:** yes (by: a separately-dispatched Section E "Mobile ↔ Mobile" subagent — see Verify detail below; originally reproduced twice by the finder, in two separate runs/scenarios, see below)
- **Reproduction:**
  1. Create a throwaway spreadsheet/tab, grant anonymous edit access (or use 3+ distinct authenticated users with edit access).
  2. Open 3+ real browser clients (distinct browser contexts) on the SAME tab_id.
  3. Have all 3+ clients, at nearly the same instant (`Promise.all`, no stagger), select the SAME cell and commit a distinct value each (click cell, Enter to begin edit, type value, Enter to commit).
  4. Wait out the server's debounce window (~7s) for propagation/persistence to settle.
  5. Compare: (a) the value shown in each client's own DOM for that cell, (b) the authoritative persisted value in `spreadsheet_history` (latest `sequence` row) for that tab_id.
  - Confirmed twice: run 1 (5 clients, cell `B1`, tab 1796) — 4 of 5 clients converged on `VALUE_CAROL`, matching the DB's persisted final state, but the 5th client (`Carol`, whose own edit `VALUE_CAROL` is the one that actually won/persisted) displayed `VALUE_ALICE` instead — its own screen never showed the value it had just typed and that in fact became the true final document state. Run 2 (3 clients, plain cell `D1`, no `ACTIONGROUP` involved, isolating the mechanism from that feature): 2 of 3 clients converged on `RACE_THREE` (matching the DB), but the 3rd client (`Race3`, author of the winning `RACE_THREE` edit) displayed `RACE_ONE` — again the stale value of a losing peer, not its own winning edit, and not the true document state.
- **Expected vs. actual:** Expected: after a same-cell race settles, every connected client's own view converges to the same value as the authoritative persisted document (last-write-wins, but consistently so for every viewer, including the "winner"). Actual: the client whose own edit becomes the authoritative value can be left permanently displaying a stale, different value (a losing peer's earlier edit) for that exact cell — diverging from both the DB's persisted state and every other client's (correctly-converged) view. It does not self-correct without a reload/reconnect (which re-fetches the full `state` message) or another edit to that same cell.
- **Root cause (from reading the code, not yet fix-verified):** `ws-server/session.py`'s `_broadcast_others()` only relays an accepted `new_edit` to OTHER clients, never back to the sender (by design, to avoid echo) — so the client whose edit ends up being the server's last-processed (and thus authoritative) value for a cell never receives any message correcting/reconfirming that cell after its own local, optimistic render. Meanwhile `assets/js/grid.js`'s `applyRemote()` (wired from `assets/js/app.js`'s `onRemoteEdit: (patch) => grid.applyRemote(patch)`) unconditionally overwrites `this.cells[ref]` with whatever remote patch arrives, with no version/ordering check against a newer, not-yet-superseded LOCAL edit to that same ref. Because each client also applies its own edit locally and instantly (`setCellValue()`, well before the 400ms `EDIT_DEBOUNCE_MS` client-side send debounce in `ws.js` even fires the network send), there's a window where a peer's older edit can arrive and overwrite the local optimistic render — and because the sender never gets its own edit echoed back, that overwrite is never subsequently corrected once the local edit actually reaches and wins at the server.
- **Verify detail:** Independently reproduced from scratch by a different subagent (dispatched separately for the Section E "Mobile ↔ Mobile" matrix item, not told this entry's conclusion beforehand — this file was read only after already reproducing and root-causing the bug independently). Confirms the bug is broader than "3+ clients, rapid-fire" implies: it reproduces with just **2** clients, **1** simultaneous edit each (no rapid-fire needed), and with **both** clients mobile-emulated (real Chromium, iPhone 13 + Pixel 5 device profiles via `puppeteer-core`, real WS against the actual `blanket-ws` server at `https://church.dogmanjr.net/blanket/`, not local `php -S` — that dev server can't proxy the WS upgrade at all, confirmed separately, so real-WS mobile testing needs the live site or a real Apache+`mod_proxy_wstunnel` path). Isolated repro: two mobile clients (2 distinct throwaway users, both `edit` access) simultaneously typed into the same never-before-touched cell (`E1`) — one `AAAA`, the other `BBBB` — then the test waited 8s (past the 5s server debounce) with NO further interaction. Client A's DOM settled on `BBBB`; client B's DOM settled on `AAAA`; `spreadsheet_history` (latest sequence for the tab) showed the authoritative persisted value was `AAAA`. So client A (whose display showed `BBBB`) was permanently wrong relative to both the DB and client B's own (correct) view, with no self-correction — and a hard reload of client B (which re-fetches full `state`) correctly showed `AAAA`, confirming the DB value was truth and client A was the one left stale. This matches this entry's documented mechanism exactly (the "winning" sender never gets its own edit echoed back, so a peer's earlier, now-superseded broadcast can permanently squat on the winning sender's own cell display). Also separately confirmed via the same setup: presence roster accuracy/distinct colors/clean disconnect-removal, and correct lossless sync for edits to *different* cells, all worked correctly for the Mobile↔Mobile pairing — this same-cell race is the one deviation found in that matrix cell. Given it reproduces from a single simultaneous pair of edits (not requiring 3+ way rapid-fire), and directly undermines confidence in the headline real-time-collab feature with a silent, non-obvious, non-self-healing symptom, recommend Fernando reconsider whether `medium` still fits, or whether this is closer to `high` — leaving the actual severity field as originally logged rather than unilaterally overriding it.
- **Status:** open

### [002] Cell and row/column-header context menus have no mobile touch equivalent — Insert/Delete row/column completely unreachable via touch; Cut/Paste/Clear-contents unreachable outside a physical keyboard

- **Area:** Mobile touch / feature parity (Section B Part 2 — context menus)
- **Severity:** high
- **Found by:** Section B Part 2 feature-parity subagent, "context menus" item
- **Verified:** no — see detail (not yet independently re-verified by a second agent; logging now per the rule that an unconfirmed finding still gets recorded rather than discarded)
- **Reproduction:**
  1. `grid.js`'s only listener that opens a context menu is `this.container.addEventListener('contextmenu', ...)` (`_onContextMenu`, grid.js ~line 166/996), which dispatches a `gridcontextmenu` CustomEvent that `app.js` (~line 830) turns into the actual `.tab-menu` popup via `showCellContextMenu`/`showHeaderContextMenu`. There is no other code path anywhere in `assets/js/*.js` that ever opens this menu, and no `long-press`/`longpress` handling exists in the codebase at all (confirmed via `grep -ri "long.press" assets/js/*.js` — zero matches).
  2. The grid's own touch handling (`_onTouchStart`/`_armTouchDragCandidate`/`_onTouchMove`/`_onTouchEnd`, grid.js ~lines 1134-1232) is the only thing that reacts to a finger held still on a cell/row-header/col-header: after `TOUCH_DRAG_ARM_MS` (350ms) of no movement it "arms" and starts a **drag-select** (`this._select()`/`selectWholeRow()`/`selectWholeColumn()`), not a context menu. It never dispatches `contextmenu` or `gridcontextmenu`.
  3. Reproduced empirically with a real Chromium instance (`puppeteer-core`, mobile emulation: 390×844 viewport, `isMobile:true`, `hasTouch:true`, iOS Safari UA) against a throwaway spreadsheet/tab: dispatched a genuine touch sequence on a cell — real `touchstart`, held 700ms (past the 350ms arm timer, no movement), then real `touchend` — with `gridcontextmenu`/`contextmenu` listeners armed beforehand. Result: **zero** `contextmenu`/`gridcontextmenu` events fired, and no menu ever appeared; the only observable effect was the cell becoming selected (the drag-select-arm path claiming the gesture), confirmed via a screenshot showing the formula bar's reference label updating to `A1` with the cell highlighted, no menu present.
  4. As a control, manually dispatching a synthetic native `contextmenu` `MouseEvent` at the same cell (simulating what a right-click, or a browser that DID synthesize one from touch, would produce) correctly opened the real menu (`Copy`/`Cut`/`Paste`/`Clear contents`/`Show browser menu` for a cell; `Insert N row(s) above/below`+`Delete row(s)` for a row header; `Insert N column(s) left/right`+`Delete column(s)` for a column header) with reasonably touch-sized tap targets — so the menu UI itself is fine, the problem is purely that nothing on a touchscreen ever triggers it.
  5. Checked for any alternative UI path to the same actions: `grep`'d `assets/js/app.js` for every call site of `insertRowsAt`/`deleteRowsAt`/`insertColumnsAt`/`deleteColumnsAt`/`_cutSelectionToClipboard`/`_pasteClipboardAtSelection`/`_clearSelection` — the ONLY call sites for all of them are inside `showCellContextMenu`/`showHeaderContextMenu` themselves. No toolbar button, no "+" row/column affordance, nothing in `index.html`, offers any of these anywhere else. The one keyboard-shortcut fallback that exists (`grid.js`'s `_onKeyDown`: Ctrl/Cmd+C, Ctrl/Cmd+V, and Delete/Backspace-clears-selection-when-not-editing) requires a physical keyboard and is irrelevant to a touch-only device with no attached keyboard — Delete/Backspace-while-selected-but-not-editing in particular has no virtual-keyboard equivalent at all, since the on-screen keyboard on mobile only appears once a cell is already in edit mode (double-tap → `_beginEdit`), by which point `_onKeyDown`'s early `if (this.editingInput ...) return;` means that whole branch (and thus `_clearSelection()`) is unreachable anyway.
- **Expected vs. actual:** Expected: per the testing plan's own framing, mobile touch users should either have a working equivalent (e.g. long-press-to-open-menu, matching Google Sheets/Excel mobile — which the code's own comments elsewhere explicitly reference as the intended UX model for touch gestures) or, failing that, some other UI path to the same actions. Actual: there is no long-press-to-context-menu implementation at all, and it was empirically confirmed that a real touch long-press does not open the menu (the app's own 350ms hold-to-drag-select timer claims the gesture first, before any native browser long-press-to-contextmenu synthesis could plausibly fire — and real iOS Safari, the app's actual explicit mobile target per Section B, does not reliably synthesize `contextmenu` from touch on ordinary elements at all, a long-documented WebKit limitation). The consequence: **Insert row above/below, Insert column left/right, and Delete row/column are completely unreachable by a touch-only mobile user** — there is no alternative anywhere in the app. **Cut, Paste, and "Clear contents" (for anything beyond a single cell already in edit mode) are also unreachable via touch alone** — the only alternative (Ctrl+C/V/Delete/Backspace) requires a physical keyboard. A mobile user CAN approximate clearing a single cell by double-tapping into edit mode and manually blanking the field via the virtual keyboard, but cannot clear a multi-cell range in one action, and has no way at all to insert or delete a row/column.
- **Verify detail:** Not yet independently re-verified by a second agent — logged per the rule that an unconfirmed finding is still recorded (not discarded) with `Verified: no`.
- **Status:** open

### [003] Edit committed right before (not after) a WS disconnect is silently and permanently lost — REST autosave fallback doesn't catch it

- **Area:** Section F (a) — WS-drop-mid-edit / REST autosave fallback
- **Severity:** high
- **Found by:** Section F automated extra-scope sweep
- **Verified:** yes (by: an independent second agent — CONFIRMED)
- **Reproduction:**
  1. Set up: throwaway user `wftest_<suffix>` (created via `bin/create-admin.php`), throwaway spreadsheet + tab, logged in against `https://church.dogmanjr.net/blanket/` (real WS server) via `puppeteer-core` + real headless Chromium (per Ground Rules workaround).
  2. Instrument the page (`evaluateOnNewDocument`) to wrap `window.WebSocket` so the app's real WS instance can be captured and later closed programmatically, simulating a genuine mid-session network drop.
  3. Load the tab; confirm status shows "Live" (WS connected).
  4. Use a real Puppeteer click (`page.click`, not `element.click()`) to select a cell, type a distinct value, press Enter to commit (this fires `grid.onChange` → `socket.queueEdit(patch)` [starts 400ms debounce] and `localSaveFallbackTimer()` [no-ops because `isConnected()` is still true at this instant]).
  5. ~60ms later (well inside the 400ms debounce window), call the captured WebSocket instance's `.close()` to sever the connection.
  6. Wait several seconds with NO further edit made. Status dot correctly flips to "Offline", but the "Saved X ago" label never updates and no `/api/tabs/{id}/save` request is ever issued (confirmed via a `fetch()` wrapper logging every request the page made).
  7. Query `spreadsheet_history` directly for that `tab_id`: only the original tab-creation row exists; no new row was ever written containing the typed value — the edit is genuinely and permanently lost (a page reload would show the pre-edit state).
  8. Control test: if a SECOND edit is made after the drop (while status already shows "Offline"), that second `onChange` call correctly sees `isConnected()===false`, schedules the REST fallback, and the resulting save persists BOTH the first (previously stranded) and second edit's values together, since the fallback POSTs the whole in-memory `grid.cells`, not just a delta. This confirms the loss is specific to the no-further-edit case, and that `grid.cells` itself isn't corrupted — only the persistence step for that first edit is dropped.
- **Expected vs. actual:** Expected: per the documented `localSaveFallbackTimer` design (`assets/js/app.js` comments), any edit that isn't successfully carried by the live WS session should fall back to the REST autosave path so no edit is ever silently lost, regardless of exactly when in the WS debounce window the connection happens to drop. Actual: an edit committed right before (but not after) a WS disconnect is neither sent over WS nor picked up by the REST fallback, and is never persisted to `spreadsheet_history` at all unless the user happens to make another edit afterward. Nothing in the UI indicates this specific edit was lost — the cell shows the typed value locally and the connection status only says "Offline", which reads as "not currently live", not "you just lost data".
- **Verify detail:** FULLY REPRODUCED — the reported bug is real, confirmed both by code inspection and by live end-to-end reproduction against the real production WS server (`https://church.dogmanjr.net/blanket/`), independently from scratch.

  Code-level confirmation (root cause matches the report exactly): `assets/js/ws.js` `_flushEdit()` (called only after the 400ms `EDIT_DEBOUNCE_MS` debounce fires) gates solely on `!this.isConnected()` evaluated AT FLUSH TIME, and if disconnected it just `return`s — `_pendingPatch` is never cleared, never retried, and no signal is emitted back to the app. `assets/js/app.js` `localSaveFallbackTimer()` gates on `if (socket.isConnected()) return;` evaluated synchronously at the moment `onChange` fires (i.e. at edit time, ~0ms after commit) — before the WS debounce has had any chance to fail. The `onStatus` handler's `'disconnected'` branch (app.js) only updates the status-dot UI; it never calls `localSaveFallbackTimer()` or otherwise re-checks/requeues any edit that was mid-flight in the WS debounce window. There is no requeue/retry path anywhere in either file.

  Live reproduction (throwaway data only, per Ground Rules — user `wftest_6403d12d` id=209, spreadsheet id=5724, tab id=1844, created via `bin/create-admin.php` + REST API; all deleted by exact ID afterward, verified 0 rows remaining): used `puppeteer-core` + real headless Chromium (per the documented `LD_LIBRARY_PATH` workaround) with `evaluateOnNewDocument` wrapping `window.WebSocket` in a Proxy to capture the app's real socket instance. Pre-seeded `blanket_token` localStorage + `blanket_name` cookie to skip login/welcome-modal, navigated to `#/sheets/5724/tabs/1844`, confirmed status showed "Live". Real `page.mouse.click` on cell A1, typed `WSDROP_<ts>`, pressed Enter (fires `grid.onChange` → `queueEdit` + `localSaveFallbackTimer()`, the latter no-oping since `isConnected()` was still true). 62ms later (well inside the 400ms window) called `.close()` on the captured WS instance. Status flipped to "Offline" ~71ms after commit. Waited 6s with no further edit: a full network-request log showed exactly zero save-related requests (no `POST /api/tabs/1844/save`, only earlier GETs). Queried `spreadsheet_history` directly for `tab_id=1844`: only the original tab-creation row (`cells: {}`) existed — the typed A1 value was never persisted anywhere, confirmed permanently lost. The cell's DOM still showed the typed value locally, matching the report's point that nothing in the UI signals this specific loss beyond the ambiguous "Offline" status.

  Control test (separate run, edits to B1 then C1 to avoid conflating with the first run): first edit dropped via WS close at +63ms, status confirmed Offline at +71ms; then a second edit was made while already offline. That second `onChange` correctly saw `isConnected()===false`, scheduled the REST fallback, and after ~1.2s+ debounce a single `POST /api/tabs/1844/save` fired containing BOTH values in one payload: `{"cells":{"B1":{"value":"FIRST_..."},"C1":{"value":"SECOND_..."}}}`. Verified in `spreadsheet_history` (row id 13465) — both edits landed together in one history row, confirming `grid.cells` itself isn't corrupted; only the persistence step for the stranded first edit was dropped, and it only survives by "hitching a ride" on a later edit's full-state save.

  This exactly matches every claim in the original report, including severity reasoning: an edit made right before (not after) a WS drop is silently and permanently lost unless the user happens to edit again afterward, with no requeue/retry mechanism connecting the WS debounce failure back to the REST fallback path.

  Relevant files: `/home/claude/blanket/assets/js/ws.js` (lines 9, 126-130, 181-186), `/home/claude/blanket/assets/js/app.js` (lines 507-510, 722-739, 763-778).
- **Status:** open

### [004] Object-typed cell `value` renders as literal "[object Object]" instead of degrading gracefully like sibling malformed shapes

- **Area:** Section F (b) — malformed/corrupted cell value: object-typed `value` renders as literal "[object Object]"
- **Severity:** low
- **Found by:** Section F automated extra-scope sweep
- **Verified:** yes (by: an independent second agent — CONFIRMED)
- **Reproduction:**
  1. Create a throwaway spreadsheet/tab, then insert a new `spreadsheet_history` row directly via PDO (bypassing the app entirely) for that `tab_id` containing a `cells` map with a dozen malformed shapes, including `"A3": {"value": {"nested": "object-not-string"}}`.
  2. Load the tab in real headless Chromium (`puppeteer-core` against the live site) with console/pageerror listeners attached.
  3. Result: 0 pageerrors, 1 unrelated 404 (browser's own root-level favicon.ico request, confirmed unrelated to this app). The grid renders fully (120 cells), and every other malformed cell (null, bare string, bare number, null-as-whole-entry, array, truncated formula `=SUM(A1:A100`, unknown function `=NONEXISTENTFUNC(A1)`, ACTIONGROUP with an invalid cell ref, non-numeric fontSize, non-numeric merge dimensions) rendered gracefully — either blank, coerced-to-string, or #ERROR.
  4. Only A3 (the object-valued cell) rendered the literal text "[object Object]" instead of blank/#ERROR.
  5. Confirmed this is cosmetic only, not a functional break: clicking A3 still selects it correctly, and typing a character still opens the inline editor and correctly overwrites the garbage value (shows the newly typed character, not the old "[object Object]" text) — same behavior as any other cell. A sibling unaffected cell (D5) was independently edited and saved successfully, proving the malformed data on this tab did not break rendering, editing, or persistence for the rest of the sheet.
- **Expected vs. actual:** Expected: a cell with a fundamentally malformed `value` type (an object instead of the required string) should render as blank or #ERROR, consistent with how every other malformed cell shape on the same tab (null, bare string/number/array entries) was actually handled. Actual: this one shape falls through to whatever string-coercion path the renderer uses (effectively `String(cellValue)` or template-literal interpolation of a raw object), producing the literal text "[object Object]" visible to any real user who encounters this pre-existing bad data — confusing/unprofessional-looking but not a crash, not read-only, and does not affect any other cell on the tab.
- **Verify detail:** CONFIRMED — reproduced independently from scratch, matching the reported finding exactly.

  Method: Created fresh throwaway user (`wftest_verify_fdd42198`, id 208), spreadsheet (id 5722, guid `d82085e5-876e-11f1-9c55-b8ac6f88121e`), and tab (id 1841) via direct PDO/MySQL insert bypassing the app, then inserted a `spreadsheet_history` row (id 13449) with a `cells` map containing 12 malformed shapes plus a normal control cell D5: null value, bare-string cell entry, object-typed value (`{"nested":"..."}`), bare-number cell entry, null-as-whole-entry, array cell entry, truncated formula, unknown-function formula, ACTIONGROUP with invalid cell ref, non-numeric fontSize, non-numeric merge dims, and a boolean value. Loaded the tab in real headless Chromium (chromium-1228 via puppeteer-core, per TESTING_PLAN.md's Ground Rules workaround) with console/pageerror/response listeners attached, logged in as the owner, navigated to the guid URL.

  Result: 0 pageerrors, exactly 1 unrelated 404 (the browser's own root-level favicon.ico request) — matches the original report precisely. Grid rendered fully. Cell-by-cell dump plus a screenshot confirmed: A1 (null value): blank; A2 (bare-string cell entry): blank; A3 (object-typed value `{"nested":"..."}`): literal text "[object Object]" — THE BUG; A4 (bare-number cell entry): blank; A5 (null-as-whole-entry): blank; A6 (array cell entry): blank; A7 (truncated formula): #ERROR; A8 (unknown function): #ERROR; A9 (ACTIONGROUP, invalid cell ref): renders correctly as a clickable "Sign" button; A10 (non-numeric fontSize): renders "1" fine; A11 (non-numeric merge dims): renders "1" fine; A12 (boolean value true): coerces to "true" (correct string coercion, not "[object Object]"); D5 (control): "control-cell-should-be-fine".

  Only A3 (the one shape whose `value` is itself an object) leaked "[object Object]" — every other malformed shape degraded gracefully (blank or #ERROR), confirming the report's central claim that this is the one outlier.

  Additional finding beyond the original report: the formula/edit bar at the top also displays the literal "[object Object]" when A3 is selected (same underlying string-coercion leak surfacing in a second UI location, not just the grid cell) — worth noting to whoever fixes this, though it doesn't change the bug's classification.

  Confirmed the "cosmetic only" claims too: clicking A3 selects it correctly (class "cell-content", no crash); typing "Z" opens the inline editor with an INPUT element whose value is exactly "Z" (overwrite behavior, not "[object Object]Z" or a crash) — same as any normal cell. Independently edited and saved sibling cell D5 (typed "EDITED-BY-VERIFIER", pressed Enter) and confirmed the new text persisted and rendered — proving the malformed data on this tab did not break rendering, editing, or persistence for the rest of the sheet.

  Cleanup: all throwaway rows deleted by exact ID and verified empty afterward (`spreadsheet_history` ids 13449 and 13476 — the second from my own D5 edit save — tab id 1841, spreadsheet id 5722, user id 208). Did not touch spreadsheet id=13 or any other agent's data.

  Severity assessment: agree with "low" — confirmed cosmetic-only, no crash, no read-only lockout, no cross-cell contamination, editing/saving works normally on the same tab. This is a real, reproducible gap in the app's malformed-value handling: the string-coercion/render path for a cell's `value` doesn't guard against the value itself being a non-string JSON object, unlike every sibling malformed shape which is handled gracefully.

  Relevant files (read-only inspection, not modified): `/home/claude/blanket/TESTING_PLAN.md` (Ground Rules followed), `/home/claude/blanket/CELL_SCHEMA.md` (canonical cell shape reference).
- **Status:** open

### [005] Spreadsheet grid is completely unreachable via keyboard alone — every cell has `tabindex="-1"` and no other affordance establishes initial selection

- **Area:** Accessibility - keyboard grid entry
- **Severity:** high
- **Found by:** Section F automated extra-scope sweep
- **Verified:** yes (by: an independent second agent — CONFIRMED)
- **Reproduction:**
  1. Log in (or open an anonymous-access spreadsheet) and load a tab in a real browser.
  2. From page load, press Tab repeatedly without ever clicking/touching the grid.
  3. Observe the tab order visits toolbar buttons, tab-nav links, the formula bar, and finally a `.grid-scroll` container div (tabindex on the scroll wrapper only) — but focus never lands on any actual cell (`table.grid td`), confirmed via DOM inspection: all 120 `<td>` elements in the test tab have `tabindex="-1"` and grid.js's `this.selected` is only ever set from mousedown/touch handlers (grep for `_select(` shows every call site is inside a pointer-event listener; `_onKeyDown` early-returns when `!this.selected`).
  4. Confirm arrow keys are consequently unusable too, since `_moveSelection()` requires `this.selected` to already be a valid ref, which is unreachable without a prior mouse/touch click.
- **Expected vs. actual:** Expected: a keyboard-only user (no mouse/touch device, e.g. a switch-access or screen-reader user, or simply someone using Tab by choice/necessity) should be able to reach and select at least one cell (e.g. A1) using only the keyboard, after which the already-working arrow-key traversal takes over. Actual: there is no keyboard path into the grid at all. Tab skips over the entire table (every cell has an explicit `tabindex="-1"`), so a user without a pointing device cannot select any cell, cannot use arrow-key navigation (which depends on a selection already existing), and cannot edit any cell — the app's primary function is entirely unreachable via keyboard alone. This is more severe than the plan's premise that "arrow-key traversal is already known to work" (which was itself only verified after a mouse click established the initial selection).
- **Verify detail:** REPRODUCED — confirmed independently via both static code reading and a live real-Chromium session against the actual deployed app (`https://church.dogmanjr.net/blanket/`), with a throwaway account/spreadsheet created for the test (cleaned up afterward, exact-ID only, verified before/after).

  Static code (`assets/js/grid.js`): line 352: `td.tabIndex = -1;` — set unconditionally on every cell during grid build, no exception. Line 1382: `_onKeyDown(e) { if (this.editingInput || !this.selected) return; ...` — the entire keydown handler (arrows, Enter/F2, Delete, Ctrl+C/V, typing-to-edit) bails immediately if no cell is currently selected. Grepped every call site that sets `this.selected`/calls `_select(`: all are inside `_onMouseDown` (requires `e.button === 0`), `_onMouseMoveDrag`, `_onTouchStart`/touch handlers, row/col-header mouse handlers, or `_moveSelection` (itself only reachable from the arrow-key branch of `_onKeyDown`, which already requires `this.selected` to be truthy — i.e. it can only move an existing selection, never create the first one). No keyboard-only path establishes an initial `this.selected`. No `tabindex` attribute exists anywhere in the codebase (grid.js, app.css, HTML) on `.grid-scroll` or any wrapper — confirmed via grep. It is not opted into by the app at all.

  Live reproduction (real Chromium 1228 via puppeteer-core, per TESTING_PLAN.md's browser workaround, executablePath `/home/claude/.cache/ms-playwright/chromium-1228/chrome-linux64/chrome` with the LD_LIBRARY_PATH fix): created throwaway account `wftest_55d901e6` via `bin/create-admin.php`, logged in, created a fresh spreadsheet `a11ytest_55d901e6` (default tab, confirmed 120 `<td>` cells — matches the report's "all 120 <td> elements"). Pre-seeded the `blanket_name` cookie to skip the Welcome modal, then loaded the tab fresh (no prior click/touch anywhere). Pressed Tab 60 times from a blank body-focus state, logging `document.activeElement` each time. Tab order visited (in this exact cycle, repeating 3x over 60 presses): Rename, Manage tabs, Share, Exit buttons → tab-nav link → Bold/Italic/Underline buttons → 2 color inputs → font-family/font-size selects → Wrap/Merge/Unmerge buttons → "⋮" menu button → formula-input → "?" help button → `.grid-scroll` div (tabIndex reads -1 as a JS property but it IS a real Tab stop in Chromium — this is Chromium's native "sequential focus navigation for scrollable regions" affordance, not an explicit tabindex attribute the app set; a minor correction to the report's phrasing "tabindex on the scroll wrapper only" but it changes nothing substantive) → wraps to BODY → repeats. Not once in 60 Tab presses did focus land on any `td[data-ref]` cell. After the Tab sequence, pressed ArrowRight then ArrowDown with no prior click: active element stayed BODY, no cell selection occurred, confirming `_onKeyDown`'s early-return with no selected cell.

  Conclusion: the finding is accurate as described. A keyboard-only user (no mouse/touch) genuinely cannot select A1 or any cell, and consequently cannot use the arrow-key traversal or edit any cell — the app's core spreadsheet function is entirely unreachable via keyboard alone. Severity "high" is justified.

  All throwaway DB rows (user id 210, spreadsheet id 5725, tab id 1845, history row id 13453) were deleted by exact ID and verified gone; spreadsheet id=13 ("Test", owner fvf) was never touched.
- **Status:** needs-Fernando-decision

### [006] Most dialogs don't move focus into themselves on open and have no Escape-to-close handler

- **Area:** Accessibility - dialog focus management
- **Severity:** medium
- **Found by:** Section F automated extra-scope sweep
- **Verified:** yes (by: an independent second agent — CONFIRMED)
- **Reproduction:**
  1. In a real browser, focus the toolbar's "Share" button via Tab and press Enter/Space to open the Share dialog (confirmed reachable, since Share is a normal `.btn`).
  2. Immediately re-check `document.activeElement`: it is still the (now visually covered) Share button, not any element inside `.modal` — confirmed no `.focus()` call exists in `showShare()`'s source.
  3. Continue pressing Tab: focus proceeds through Exit, the tab-nav link, Bold/Italic/Underline, the two color inputs, font family/size selects, Wrap/Merge/Unmerge, the tab-options (⋮) button, the formula input, the formula-help (?) button, and the grid-scroll container — 16 more Tab presses — before finally reaching the Share dialog's own first control (the read-only share-link input) on the 17th Tab.
  4. Press Escape while the dialog is open: it does not close (grep confirms no dialog in app.js/grid.js listens for 'Escape'; only the formula input's own in-place edit-cancel and grid.js's cell-edit-cancel handle Escape). The only ways to close most dialogs are a backdrop click or Tab-ing the long way around to reach the Close button.
  5. By contrast, `showRenameSpreadsheet()`, `showRenameTab()`, the required Welcome name-prompt, and the USERINFO consolidated prompt all correctly call `.focus()` on their first input immediately after appending themselves to the DOM — so the defect is inconsistent across dialogs, not universal.
- **Expected vs. actual:** Expected: opening any modal dialog should move keyboard focus into the dialog immediately (ideally to its first focusable control), and Escape should be a standard way to dismiss a dismissible dialog. Background page controls that are visually covered by the modal overlay should not remain reachable/interactive via Tab while the modal is open. Actual: Share, Manage Tabs, History, Formula Help, and the tab quick-menu leave focus on the trigger element and leave the entire rest of the page's toolbar/nav in the tab order ahead of the dialog's own content, and none of them respond to Escape. A keyboard-only user opening one of these has to tab through roughly 15+ unrelated, visually-obscured controls before reaching anything inside the dialog they just opened, and has no keyboard shortcut to close it again.
- **Verify detail:** REPRODUCED — confirmed independently, both by direct code inspection and by a live real-Chromium browser test.

  Code inspection (all in `/home/claude/blanket/assets/js/app.js`): `showShare()` (line 1051-1139): no `.focus()` call anywhere; dialog only closes via backdrop click (`e.target === dialog`) or the Close button (line 1134). No Escape handling. `showHistory()` (line 849-876): same pattern — no focus, no Escape, backdrop/Close-button only. `showFormulaHelp()` (line 957-1002): no focus, no Escape, and (by deliberate design per the code comment at line 963-965) not even a Close button — backdrop click is the *only* dismissal. `showManageTabs()` (line 1383-1490): no focus, no Escape. `showTabMenu()` (line 1255-1301): the per-tab quick menu — no `.focus()` on any menu item, dismissal only via an outside-click document listener (`onDocClick`), no Escape handling. Confirmed via grep that only two Escape handlers exist anywhere in the app: `app.js:531` (formula-bar in-place edit cancel) and `grid.js:1312` (grid cell-edit cancel) — neither is a dialog-level handler, and grep for 'Escape' across app.js/grid.js turns up nothing else. By contrast, confirmed `showRenameSpreadsheet()` (line 1334), `showRenameTab()` (line 1373), `promptForNameIfNeeded()` (the Welcome prompt, line 1570), and `showUserInfoPrompt()` (the USERINFO consolidated prompt, line 1047) all call `.focus()` on their first input immediately after appending to the DOM — matching the report's claim that the defect is inconsistent, not universal.

  Live browser reproduction (real Chromium 1228 via puppeteer-core, per TESTING_PLAN.md's browser-testing workaround, driven against a local `php -S` dev-router instance of the actual app, with a throwaway test user/spreadsheet created and fully cleaned up by exact ID afterward): created throwaway user `a11ytest_5e9962cf` (db id 211, deleted after) and two throwaway spreadsheets (ids 5727/5728, deleted after, along with their tabs/history — all cleanup verified by SELECT returning zero rows). Logged in, opened a spreadsheet, `.focus()`'d the Share button (simulating having Tab-reached it) and pressed Enter. Confirmed the Share modal opened (`.modal` present) but `document.activeElement` remained the Share button itself, not anything inside `.modal` — exactly as claimed. Pressed Tab repeatedly and logged the full trace. It took exactly 17 Tab presses to land inside `.modal`, passing through: Exit (a), tab-nav link (tab-0), Bold, Italic, Underline, two `.btn-color` inputs, font-family select, font-size select, Wrap, Merge, Unmerge, the ⋮ tab-options button, the formula input, the ? formula-help button, and the grid-scroll container (16 controls) before the 17th Tab landed on `.share-link-input` (the dialog's own first control) — matching the reproduction steps' claimed count and control list almost exactly (the report said "16 more Tab presses" after the initial Enter, i.e. 17 total to reach the dialog's first control, which is exactly what was observed). Pressed Escape with the dialog still open: the `.modal` element was still present afterward — Escape does not close it, confirming the claim.

  Severity assessment: the "medium" claim is reasonable — this is a genuine, consistent, reproducible keyboard-accessibility defect affecting five distinct dialogs, with a working counter-example pattern already present elsewhere in the same codebase (the four dialogs that do call `.focus()`), which strengthens the case that this is an inconsistency/oversight rather than an intentional design choice, and makes the fix straightforward (add `.focus()` on the first focusable control and a shared Escape-to-dismiss handler to each of the five affected dialogs/menus).

  No code changes were made — per Ground Rules this is a Section F finding to be recorded, not fixed inline. Test artifacts (throwaway user/spreadsheets) were fully cleaned up by exact ID; the local dev PHP server used for testing was stopped.
- **Status:** needs-Fernando-decision

### [007] Icon-only toolbar buttons (Bold/Italic/Underline/⋮/?/Move left/Move right) have no `aria-label`, relying solely on `title` + an ambiguous glyph

- **Area:** Accessibility - icon-only buttons lack accessible names
- **Severity:** low
- **Found by:** Section F automated extra-scope sweep
- **Verified:** yes (by: an independent second agent — CONFIRMED)
- **Reproduction:**
  1. Inspect the toolbar DOM in a real browser: `document.querySelectorAll('button.btn-icon')` returns buttons whose only accessible-name sources are `textContent` ('B'/'I'/'U'/'⋮'/'?') and a `title` attribute ('Bold'/'Italic'/'Underline'/'Tab options'/'Formula help'); `grep -rn "aria-" assets/js/*.js` returns zero matches across the entire frontend.
  2. `title` alone is not reliably exposed to screen readers as an accessible name substitute in the way `aria-label` is, and provides no benefit to keyboard users (no native focus-triggered tooltip in most browsers), so these controls are more ambiguous than necessary for assistive-tech users (e.g. '⋮' and '?' read literally as their Unicode character names by some screen readers rather than 'Tab options'/'Formula help').
- **Expected vs. actual:** Expected: icon/glyph-only interactive controls should carry an explicit `aria-label` (or visually-hidden text) stating their function, per the plan's own framing of "a quick note even if not a blocking release item." Actual: no `aria-label`/`aria-*` attributes exist anywhere in the frontend JS; icon-only buttons depend on `title` plus a terse glyph/letter as their only accessible name. Noted as low severity per the plan, not treated as release-blocking.
- **Verify detail:** Reproduced by independent static inspection of the actual source (not just trusting the summary).

  Method: read TESTING_PLAN.md Ground Rules first (real-browser requirement applies only to rendering/layout/timing-sensitive findings; this is a static-markup/accessible-name-shape claim, so source inspection is the correct verification method, not a live DOM/browser check).

  Findings, matching the reproduction steps exactly: `grep -rn "aria-" assets/js/*.js` returns zero matches — confirmed. Extended the check to `grep -rn "aria-" assets/` (all JS/CSS/HTML under assets/) and to `sr-only`/`visually-hidden`/`screen-reader` anywhere in the repo's frontend — all zero matches. There is no `aria-label`, no `role=`, and no visually-hidden-text technique anywhere in the frontend. `grep -n "btn-icon" assets/js/app.js` finds exactly 7 icon-only buttons, each with only a `title` attribute plus a terse glyph/letter as its text content, no `aria-label`: line 538 `formulaHelpBtn` (`title: 'Formula help'`, text '?'); line 615 `tabMenuBtn` (`title: 'Tab options'`, text '⋮'); line 654 Bold (`title: 'Bold'`, text 'B'); line 655 Italic (`title: 'Italic'`, text 'I'); line 656 Underline (`title: 'Underline'`, text 'U'); line 1400 Move-left / tab reorder (`title: 'Move left'`); line 1419 Move-right / tab reorder (`title: 'Move right'`).

  This is a superset of the buttons named in the original report (which listed Bold/Italic/Underline/tab-options/formula-help) — two more icon-only buttons (Move left/Move right) have the identical pattern, making the underlying issue slightly broader than described, though still low severity as claimed.

  Every button/attribute/glyph named in the reproduction steps was verified present in the actual codebase at the exact cited locations. The claim "no aria-* anywhere in the frontend" is exactly true. This is a real, reproducible, low-severity accessibility gap (icon-only controls with no `aria-label`/visually-hidden text, relying solely on `title` + an ambiguous glyph/letter for their accessible name).

  Files: `/home/claude/blanket/assets/js/app.js` (lines 538, 615, 654-656, 1400, 1419).
- **Status:** needs-Fernando-decision
